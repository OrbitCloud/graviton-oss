from typing import List, Optional

import pulumi

from orbitcloud_graviton.pulumi_lib import (
    AzureStack,
    EntraStack,
    PulumiConfig,
    generate_stack_schema,
    get_azure_stack,
    get_entra_stack,
)


class {{base_name}}Config(PulumiConfig):
    pass


def deploy() -> None:
    generate_stack_schema(model={{base_name}}Config, output_file=".stack_schema.json")

    config: {{base_name}}Config = {{base_name}}Config.model_validate({})
    entra_config: EntraStack = EntraStack.model_validate({})

    # Get Azure Stack and export resource group
    stack: AzureStack = get_azure_stack()
    entra_config: EntraStack = get_entra_stack()
