# Task 02: MCP Server Configuration

> Status: done

## Goal
Create `.mcp.json` at project root with Context7 and Fetch MCP server definitions.

## Acceptance Criteria
- [ ] `.mcp.json` exists at project root
- [ ] Contains context7 and fetch server configs
- [ ] JSON is valid (`python -m json.tool`)
